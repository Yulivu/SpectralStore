# Preliminary Experiments

Preliminary experiments are small enough to run locally and are meant to answer:

- does the full pipeline run,
- do baseline comparisons execute,
- are metrics written to the expected result directory,
- are the results plausible enough to guide the next implementation step.

These are not final paper-scale experiments.
