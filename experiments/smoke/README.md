# Smoke Experiments

Smoke experiments are tiny end-to-end workflows. They are useful after code
changes because they verify that imports, compressors, query execution, metrics,
and result writing still work together.
